version: '3'

services:
  traefik:
    image: traefik:v2.5.3
    container_name: traefik
    ports:
      - "8085:80"
      - "8086:8080"
    volumes:
      # 绑定我们的配置目录
      - "./config:/etc/traefik"
      - "/var/log/traefik:/var/log/traefik"

